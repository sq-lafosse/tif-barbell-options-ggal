# Dependencias del proyecto — TIF Barbell Options GGAL
# Python >= 3.11
#
# Instalar con:  pip install -r requirements.txt
# Actualizar con:  pip freeze > requirements.txt  (solo en el entorno del proyecto)

# --- Manipulación y análisis de datos ---
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0          # optimización para volatilidad implícita por strike

# --- Visualización ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Fuentes de datos de mercado ---
yfinance>=0.2.30               # ADR GGAL (NYSE), Merval y T-Bills 3M (^IRX)

# --- Configuración ---
pyyaml>=6.0                    # lectura de config.yaml

# --- Formatos de archivo ---
pyarrow>=14.0.0                 # lectura/escritura de Parquet (pandas.to_parquet)

# --- Testing ---
pytest>=7.4.0

# --- Notebooks ---
jupyter>=1.0.0
